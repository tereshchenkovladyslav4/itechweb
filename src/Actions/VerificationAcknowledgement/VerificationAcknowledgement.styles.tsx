import makeStyles from '@mui/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  button: {
    height: "100%",
    width: "100%",
    fontSize: 20,
    textAlign: "center",
    marginBottom: "5px",
  },
}));
