import { Button } from "@mui/material";

import withStyles from '@mui/styles/withStyles';

const WarningButton = withStyles({ root: { backgroundColor: 'red' } })(Button);

export default WarningButton;
